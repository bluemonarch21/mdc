# Music Difficulty Classification

### packages

* `mdc-django` – web server written with Django
* `mdc-fastapi` – web server written with fastapi
* `mdc-ui` – React web ui 
* `mdc-extractor` – feature extractor written in Python
